// Lightswind's registry components import cn from "@/components/lib/utils"
// (their prescribed project structure). Re-export our existing helper so
// those files can stay verbatim.
export { cn } from "@/lib/utils";
